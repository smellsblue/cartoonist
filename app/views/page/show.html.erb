<% content_for :title, @page.title %>

<% if @page.has_comments? %>
  <% enable_disqus! :path => "#{@page.path}", :title => "#{@page.title}", :category => CartoonistConfig[:disqus_page_category] %>
<% end %>

<%= markdown @page.content %>
