<% tweet = Tweet.tweet_for entity %>
<% if tweet.tweet_style != :disabled %>
  <%= form_tag tweet.form_path, :method => tweet.form_method do %>
    <input type="hidden" name="entity_id" value="<%= tweet.entity_id %>" />
    <input type="hidden" name="entity_type" value="<%= tweet.entity_type %>" />

    <p>
      <textarea name="tweet" rows="2" cols="100"><%= tweet.tweet %></textarea>
    </p>

    <p>
      <input type="submit" name="save" value="Save" />
    </p>

    <p>
      <input type="submit" name="tweet_now" value="Tweet Now" />
    </p>
  <% end %>
  TODO: Don't allow save if tweeted, disabled unless allow_save?
  TODO: Don't allow tweet now if tweeted or entity is unposted, disabled unless allow_tweet_now?
  TODO: Redirect back to edit using a new entity_edit_url method
<% end %>
